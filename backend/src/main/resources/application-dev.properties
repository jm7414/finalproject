spring.config.activate.on-profile=dev

# 로컬 개발용 DB 설정
# DataSourceConfig.java가 자동으로 처리하므로 환경변수만 설정하면 됩니다.
# 기본값: localhost:5432/finalproject (postgres/rootroot)
# 필요시 환경변수로 덮어쓰기: DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD

# 로컬 HTTPS 설정 (로컬 인증서 사용)
server.port=8080
server.ssl.enabled=true
server.ssl.key-store=classpath:localhost+1.p12
server.ssl.key-store-password=changeit
server.ssl.key-store-type=PKCS12

# 프론트엔드 도메인 (로컬 Vite)
DOMAIN=localhost:5173

# Tmap API 설정
tmap.api.key=${TMAP_API_KEY:3EkdQeGupP3Y0mJO1SuVC31mEYHXemG4hJMIET47}
tmap.api.base-url=https://apis.openapi.sk.com/tmap

# 파일 업로드 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB





# 로컬용 스프링 실행 시 반드시 아래 코드로 실행할 것
#cd C:\LX\final-project-server\backend
#$env:SPRING_PROFILES_ACTIVE="dev"; .\mvnw spring-boot:run